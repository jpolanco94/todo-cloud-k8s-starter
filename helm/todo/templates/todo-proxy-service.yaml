apiVersion: v1
kind: Service
metadata:
  namespace: {{ template "todo.fullname" . }}-todo
  name: todo-proxy
  labels:
    todo-app-service: todo-proxy
spec:
  type: LoadBalancer
  selector:
    todo-app-pod: todo-proxy
  ports:
  - name: "8090"
    port: 8090
    targetPort: 80

